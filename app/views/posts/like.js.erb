$("#like_<%= @post.id %>").removeClass('glyphicon-heart-empty').addClass('glyphicon-heart');
$("#like_<%= @post.id %>").attr("href", "<%= unlike_post_path @post.id %>");
$("#likes_<%= @post.id %>").replaceWith("<%= j (render partial: 'posts/likes', locals: { post: @post} ) %>");
