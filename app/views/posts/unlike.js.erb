$("#like_<%= @post.id %>").removeClass('glyphicon-heart').addClass('glyphicon-heart-empty');
$("#like_<%= @post.id %>").attr("href", "<%= like_post_path @post.id %>");
$("#likes_<%= @post.id %>").replaceWith("<%= j (render partial: 'posts/likes', locals: { post: @post} ) %>");
